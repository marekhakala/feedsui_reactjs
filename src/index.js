
import React from "react";
import ReactDOM from "react-dom";
import ReactApplication from "./reactapplication";

ReactDOM.render(
    <ReactApplication
      id="application" />, document.getElementById("root"));
